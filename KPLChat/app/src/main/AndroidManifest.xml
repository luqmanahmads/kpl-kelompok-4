<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.study.dwika.kplchat">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".menu.MenuActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
        <activity
            android:name=".login.LoginActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">



        </activity>

        <activity android:name="com.study.dwika.kplchat.register.RegisterActivity" />
        <activity android:name=".addfriend.AddFriendActivity" />
        <activity android:name=".conversationdetail.ConversationDetailActivity" />
        <activity android:name=".addmember.AddMemberActivity" />

        <activity android:name=".chatroom.ChatRoomActivity">


        </activity>





        <service
            android:name=".service.ReceiverService"
            android:exported="true">
        </service>

    </application>

</manifest>